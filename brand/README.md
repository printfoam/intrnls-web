# intrnls brand tokens — SYNCED COPY (do not edit)

These files are **synced** from the canonical source in the private comms repo
(`identity/tokens/` — run `identity/tokens/sync-to-repos.sh` there to refresh).

Edit the canon, not this copy. Style through the CSS variables
(`--bg`, `--fg`, `--accent`, `--gradient`, `--go`, `--state-*`) — never raw hexes,
never a second palette. Doctrine: the identity repo's `PALETTE.md`.
